# MySQL Database Configuration
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=150506
DB_NAME=tech_jobs_db

# Fallback Settings (SQLite fallback if MySQL connection fails or is offline)
ENABLE_SQLITE_FALLBACK=True
SQLITE_DB_PATH=tech_jobs_fallback.db
